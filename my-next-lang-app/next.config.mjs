/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.migrosone.com',"encrypted-tbn0.gstatic.com","cdn03.ciceksepeti.com"],
    },
};

export default nextConfig;
